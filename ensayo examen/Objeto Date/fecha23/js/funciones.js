/*cortarFecha = () => {

	f = 0

	fecha = fecha.split("/");

	fecha = new Date(fecha [2], (fecha[1]-1), fecha [0]).getTime();

	fecha = new Date(fecha);

	f = fecha.toLocaleDateString();

	return f;*/